for (var number = 6; number < 20; number=number+2) 
{
  function exponential(number) 
  {
    if (isEven=true) 
    {
    return number * number;
    }
  }
  function isEven(number) 
  {
    return number % 2 === 0;
  }
    console.log(`The exponential of ` + number + " is " + exponential(number))
}

